BEGIN;

SELECT plan(0); --adjust this number to be the number of tests to run (or else it will fail)

-- Create

-- Read (not necessary because the other tests already use SELECT statements)

-- Update

-- Delete

SELECT finish();

ROLLBACK;
